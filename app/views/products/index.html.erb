<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <a href="/products/<%= product.id %>"><%= product.title %></a> -
      Category: <%= product.category.name %>
    </li>
  <% end %>
</ul>
